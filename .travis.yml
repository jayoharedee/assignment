#https://github.com/MuskanBatra/course-material

language: node_js

node_js:
  - "10.9.0"

cache: npm

env:
  - NOW_TOKEN=nklp0EQ0KDQms04SrKbQqPo4

before_install:
  - "npm config set spin false"
  - npm i -g now

install:
  - npm install

script:
  - npm run build
  - npm run test

deploy:
  provider: script
  script: "scripts/deploy.sh"  
  skip_cleanup: true