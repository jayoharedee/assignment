#https://github.com/gouravkhurana10/course-material

language: node_js

node_js:
  - "10.9.0"

cache: npm

env:
  - NOW_TOKEN=GmwTkM5BLzFE3CNQWAKd4y7W

before_install:
  - "npm config set spin false"
  - npm i -g now

install:
  - npm install

script:
  - npm run build
  - npm run test

deploy:
  provider: script
  script: "scripts/deploy.sh"  
  skip_cleanup: true

